class Pessoa {

    nome: string;
    idade: number;

    constructor(nome: string, idade: number) {
        this.nome = nome;
        this.idade = idade;
    }

}

const pessoa = new Pessoa("Ricardo", 15);
console.log(pessoa.nome);
console.log(pessoa.idade);
